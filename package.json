{
  "main": "index.jsx",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node esbuild.config.js",
    "start": "node esbuild.config.js -w"
  },
  "devDependencies": {
    "esbuild": "^0.13.2",
    "esbuild-serve": "^1.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
